<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="description" content="DecibelX Template">
   <meta name="keywords" content="DecibelX, audio, headphones, speakers">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <meta http-equiv="X-UA-Compatible" content="ie=edge">
   <title>DecibelX</title>
   <link rel="icon" href="/img/icon.png" type="image/png">
   <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap"
      rel="stylesheet">
   <link rel="stylesheet" href="css/bootstrap.min.css" type="text/css">
   <link rel="stylesheet" href="css/font-awesome.min.css" type="text/css">
   <link rel="stylesheet" href="css/elegant-icons.css" type="text/css">
   <link rel="stylesheet" href="css/magnific-popup.css" type="text/css">
   <link rel="stylesheet" href="css/nice-select.css" type="text/css">
   <link rel="stylesheet" href="css/owl.carousel.min.css" type="text/css">
   <link rel="stylesheet" href="css/slicknav.min.css" type="text/css">
   <link rel="stylesheet" href="css/style.css" type="text/css">
   <link rel="stylesheet" href="./user-assets/css/maind134.css">
   <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css">
   <link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.min.css">
   <style>
      /* New Header Styles */
      .header {
         background-color: #fff;
         box-shadow: 0 2px 10px rgb(0, 0, 0);
         padding: 15px 0;
         position: relative;
         z-index: 500;
      }

      .header-container {
         display: flex;
         align-items: center;
         justify-content: space-between;
         padding-top: 30px;
      }

      /* Logo with spacing to the right */
      .logo {
         display: flex;
         align-items: center;
         margin-right: 50px;
         /* Increased space between logo and nav menu */
      }

      .logo img {
         height: 55px;
      }

      /* Navigation positioned in the middle */
      nav {
         flex-grow: 1;
         /* This allows the nav to take available space */
      }

      .nav-menu {
         display: flex;
         list-style: none;
         margin: 0;
         padding: 0;
      }

      .nav-menu li {
         margin: 0 15px;
      }

      .nav-menu a {
         color: #333;
         text-decoration: none;
         font-weight: 600;
         font-size: 19px;
         transition: color 0.3s;
      }

      .nav-menu a:hover {
         color: #00c3ff;
         font-size: 20px;
      }

      /* Search bar hidden to simplify layout */
      .search-bar {
         display: none;
      }

      /* Icons with gap from navigation */
      .nav-icons {
         display: flex;
         align-items: center;
         margin-left: 50px;
         gap: 24px;
      }

      .nav-icons a {
         position: relative;
         color: #333;
         display: flex;
         align-items: center;
      }

      .nav-icons a i {
         font-size: 22px;
      }

      .nav-icons a i:hover {
         font-size: 25px;
      }

      .nav-icons .badge {
         position: absolute;
         top: -8px;
         right: -8px;
         background-color: #ff4757;
         color: white;
         border-radius: 50%;
         width: 20px;
         height: 20px;
         display: flex;
         align-items: center;
         justify-content: center;
         font-size: 12px;
         font-weight: bold;
      }

      .dropdown {
         position: relative;
         display: inline-block;
      }

      .dropdown-content {
         display: none;
         position: absolute;
         background-color: white;
         min-width: 160px;
         box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.1);
         z-index: 1000;
         right: 0;
         border-radius: 8px;
         overflow: hidden;
      }

      .dropdown-content a {
         color: #333;
         padding: 12px 16px;
         text-decoration: none;
         display: block;
         width: 100%;
         transition: background-color 0.3s;
      }

      .dropdown-content a:hover {
         background-color: #f8f8f8;
      }

      .dropdown:hover .dropdown-content {
         display: block;
      }

      .user-icon {
         cursor: pointer;
         font-size: 22px;
      }

      /* Mobile menu styles */
      .mobile-menu-toggle {
         display: none;
         cursor: pointer;
         font-size: 24px;
      }

      .mobile-menu {
         display: none;
         position: absolute;
         top: 100%;
         left: 0;
         right: 0;
         background-color: white;
         box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
         padding: 20px;
         z-index: 1000;
      }

      .mobile-menu.active {
         display: block;
      }

      .mobile-menu ul {
         list-style: none;
         padding: 0;
         margin: 0;
      }

      .mobile-menu ul li {
         margin-bottom: 15px;
      }

      .mobile-menu ul li a {
         color: #333;
         text-decoration: none;
         font-weight: 600;
         font-size: 16px;
      }

      .mobile-search {
         margin: 15px 0;
      }

      .container {
         width: 100%;
      }

      /* Responsive styles */
      @media (max-width: 991px) {
         .nav-menu {
            display: none;
         }

         .mobile-menu-toggle {
            display: block;
            margin-left: auto;
            margin-right: 15px;
         }

         .header-container {
            padding: 0 10px;
         }

         .logo {
            margin-right: 0;
         }

         .nav-icons {
            margin-left: 20px;
            gap: 15px;
         }
         
         .nav-icons a .text {
            display: none;
         }
      }

      @media (max-width: 768px) {
         .nav-icons {
            gap: 12px;
         }
      }

      @media (max-width: 480px) {
         .nav-icons {
            gap: 10px;
         }
      }

      /* Hero styles */
      .hero {
         overflow: hidden;
         position: relative;
      }

      .hero__items {
         height: 600px;
         position: relative;
         display: flex;
         align-items: center;
      }

      .hero-bg {
         position: absolute;
         top: 0;
         left: 0;
         width: 100%;
         height: 100%;
         background-size: cover;
         background-position: center;
         z-index: -1;
      }

      .hero__text {
         padding: 30px;
         background-color: rgba(0, 0, 0, 0.5);
         border-radius: 8px;
         max-width: 500px;
      }

      .hero__text h6 {
         color: #ff6b6b;
         letter-spacing: 2px;
         margin-bottom: 10px;
         font-weight: 600;
      }

      .hero__text h2 {
         color: #ffffff;
         font-size: 36px;
         line-height: 1.2;
         margin-bottom: 15px;
      }

      .hero__text p {
         color: #f1f1f1;
         margin-bottom: 20px;
      }

      .primary-btn {
         display: inline-block;
         padding: 12px 30px;
         color: #fff;
         background-color: #ff6b6b;
         text-decoration: none;
         border-radius: 4px;
         font-weight: 600;
         text-transform: uppercase;
         transition: all 0.3s ease;
      }

      .primary-btn:hover {
         background-color: #ff4c4c;
         transform: translateY(-3px);
         box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }

      /* Owl Carousel custom styles */
      .owl-dots {
         position: absolute;
         bottom: 20px;
         left: 50%;
         transform: translateX(-50%);
      }

      .owl-nav button {
         position: absolute;
         top: 50%;
         transform: translateY(-50%);
         width: 40px;
         height: 40px;
         background: rgba(255, 255, 255, 0.7) !important;
         border-radius: 50% !important;
         color: #333 !important;
         font-size: 20px !important;
         line-height: 40px !important;
         margin: 0 !important;
         transition: all 0.3s ease;
      }

      .owl-nav button:hover {
         background: rgba(255, 255, 255, 1) !important;
      }

      .owl-prev {
         left: 20px;
      }

      .owl-next {
         right: 20px;
      }

      /* Animation styles */
      .fadeIn {
         animation: fadeIn 1s ease-in-out;
      }

      .fadeOut {
         animation: fadeOut 1s ease-in-out;
      }

      @keyframes fadeIn {
         from {
            opacity: 0;
         }

         to {
            opacity: 1;
         }
      }

      @keyframes fadeOut {
         from {
            opacity: 1;
         }

         to {
            opacity: 0;
         }
      }
   </style>
</head>

<body>
   <header class="header">
      <div class="container">
         <div class="header-container">
            <!-- Logo -->
            <div class="logo">
               <a href="/"><img src="img/logo.png" alt="DecibelX"></a>
            </div>

            <!-- Navigation Menu -->
            <nav>
               <ul class="nav-menu">
                  <li><a href="/contact">Contact</a></li>
                  <li><a href="/about">About</a></li>
                  <li><a href="/products">Products</a></li>
               </ul>
            </nav>

            <!-- Mobile Menu Toggle -->
            <div class="mobile-menu-toggle">
               <i class="fa fa-bars"></i>
            </div>

            <!-- Icons -->
            <div class="nav-icons">
               <!-- Wishlist Icon -->
               <a href="/wishlist">
                  <i class="fas fa-heart" style="color: red;"></i>
                  <span class="badge">
                     <%= typeof wishlistCount !=='undefined' ? wishlistCount : 0 %>
                  </span>
               </a>

               <!-- Cart Icon -->
               <a href="/cart">
                  <i class="fas fa-shopping-cart"></i>
                  <span class="badge">
                     <%= typeof cartCount !=='undefined' ? cartCount : 0 %>
                  </span>
               </a>

               <!-- User Profile/Login -->
               <% if (locals.user) { %>
                  <div class="dropdown">
                     <a href="#" class="user-icon">
                        <i class="fas fa-user-circle"></i>
                        <span class="text" style="font-size: 16px; margin-left: 5px;">
                           <%= locals.user.name %>
                        </span>
                     </a>
                     <div class="dropdown-content">
                        <a href="/userProfile">Profile</a>
                        <a href="/logout" class="dropdown-item text-danger">Logout</a>
                     </div>
                  </div>
                  <% } else { %>
                     <a href="/login">
                        <i class="fas fa-user"></i>
                        <span class="text" style="font-size: 16px; margin-left: 5px;">Log in</span>
                     </a>
                     <% } %>
            </div>
         </div>
      </div>

      <!-- Mobile Menu -->
      <div class="mobile-menu">
         <ul>
            <li><a href="/contact">Contact</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="/products">Products</a></li>
         </ul>
      </div>
   </header>